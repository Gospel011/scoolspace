import 'package:flutter/material.dart';

class SignupUserInfo extends StatelessWidget {
  const SignupUserInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('signup user info'),
      ),
    );
  }
}
